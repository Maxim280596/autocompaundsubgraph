{
  "name": "autocompaundsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ Maxim280596/autocompaundsubgraph",
    "create-local": "graph create --node http://localhost:8020/ Maxim280596/autocompaundsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ Maxim280596/autocompaundsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Maxim280596/autocompaundsubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
